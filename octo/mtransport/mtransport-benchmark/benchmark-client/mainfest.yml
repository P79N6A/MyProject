version: v1
common:                #build 和deploy 过程公共的部分
  os: centos7     #系统中会提供列表可选
  tools:
    oracle-jdk: 7
  env:
build:
  tools:
    maven : 3.3.3
  libs :
  env :
  run :
    workDir : mtransport/mtransport-benchmark/benchmark-client
    cmd :
      - sh assembly.sh
  target:
    distDir: mtransport/mtransport-benchmark/benchmark-client/target
    files:
      - run.sh
      - benchmark-client-1.0.0-SNAPSHOT.jar
      - data/
deploy:
  tools :
  libs :
  env:
    LOGPATH : /opt/logs/mtransport-benchmark/
  targetDir : /opt/meituan/mtransport-benchmark/
  run :
    workDir : /opt/meituan/mtransport-benchmark/
    cmd :
      - sh run.sh
  checkCmd :