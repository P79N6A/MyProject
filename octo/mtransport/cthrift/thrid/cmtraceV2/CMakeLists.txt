cmake_minimum_required(VERSION 2.6)

INCLUDE_DIRECTORIES(SYSTEM ${INCLUDE_OUTPUT_PATH}/thrift)


ADD_SUBDIRECTORY(${CMTRACE_SRC_PATH} ${LIBRARY_OUTPUT_PATH})

EXECUTE_PROCESS(COMMAND find ${CMTRACE_INCLUDE_PATH} -name *.h -exec cp -t ${CMTRACE_THRID_PATH} {} \;)