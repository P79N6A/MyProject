#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import MNSCacheService
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  MNSResponse getMNSCache(string appkey, string version, string env)'
  print '  MNSResponse getMNSCache4HLB(string appkey, string version, string env)'
  print '  AppKeyListResponse getAppKeyListByBusinessLine(i32 bizCode, string env)'
  print '  HttpPropertiesResponse getHttpPropertiesByBusinessLine(i32 bizCode, string env)'
  print '  HttpPropertiesResponse getHttpPropertiesByAppkey(string appkey, string env)'
  print '  UpstreamResponse getHlbUpstream(string nginx_type, string idc_type, string env)'
  print '  SandboxResponse getSandbox(string appkey, string env)'
  print '  bool saveSandbox(string id, string env, string data)'
  print '  bool deleteSandbox(string id, string env)'
  print '   getSandboxConfig( appkeys, string env)'
  print '  MNSResponse getProvidersByIP(string ip)'
  print '  bool delProvider(string appkey, i32 env, i32 serverType, string ip, i32 port)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = MNSCacheService.Client(protocol)
transport.open()

if cmd == 'getMNSCache':
  if len(args) != 3:
    print 'getMNSCache requires 3 args'
    sys.exit(1)
  pp.pprint(client.getMNSCache(args[0],args[1],args[2],))

elif cmd == 'getMNSCache4HLB':
  if len(args) != 3:
    print 'getMNSCache4HLB requires 3 args'
    sys.exit(1)
  pp.pprint(client.getMNSCache4HLB(args[0],args[1],args[2],))

elif cmd == 'getAppKeyListByBusinessLine':
  if len(args) != 2:
    print 'getAppKeyListByBusinessLine requires 2 args'
    sys.exit(1)
  pp.pprint(client.getAppKeyListByBusinessLine(eval(args[0]),args[1],))

elif cmd == 'getHttpPropertiesByBusinessLine':
  if len(args) != 2:
    print 'getHttpPropertiesByBusinessLine requires 2 args'
    sys.exit(1)
  pp.pprint(client.getHttpPropertiesByBusinessLine(eval(args[0]),args[1],))

elif cmd == 'getHttpPropertiesByAppkey':
  if len(args) != 2:
    print 'getHttpPropertiesByAppkey requires 2 args'
    sys.exit(1)
  pp.pprint(client.getHttpPropertiesByAppkey(args[0],args[1],))

elif cmd == 'getHlbUpstream':
  if len(args) != 3:
    print 'getHlbUpstream requires 3 args'
    sys.exit(1)
  pp.pprint(client.getHlbUpstream(args[0],args[1],args[2],))

elif cmd == 'getSandbox':
  if len(args) != 2:
    print 'getSandbox requires 2 args'
    sys.exit(1)
  pp.pprint(client.getSandbox(args[0],args[1],))

elif cmd == 'saveSandbox':
  if len(args) != 3:
    print 'saveSandbox requires 3 args'
    sys.exit(1)
  pp.pprint(client.saveSandbox(args[0],args[1],args[2],))

elif cmd == 'deleteSandbox':
  if len(args) != 2:
    print 'deleteSandbox requires 2 args'
    sys.exit(1)
  pp.pprint(client.deleteSandbox(args[0],args[1],))

elif cmd == 'getSandboxConfig':
  if len(args) != 2:
    print 'getSandboxConfig requires 2 args'
    sys.exit(1)
  pp.pprint(client.getSandboxConfig(eval(args[0]),args[1],))

elif cmd == 'getProvidersByIP':
  if len(args) != 1:
    print 'getProvidersByIP requires 1 args'
    sys.exit(1)
  pp.pprint(client.getProvidersByIP(args[0],))

elif cmd == 'delProvider':
  if len(args) != 5:
    print 'delProvider requires 5 args'
    sys.exit(1)
  pp.pprint(client.delProvider(args[0],eval(args[1]),eval(args[2]),args[3],eval(args[4]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
